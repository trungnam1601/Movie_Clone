const routes = {
    home: '/',
    catalog: '/:category',
    search: '/:category/search/:keyword',
    favorite: '/favorite',
    detail: '/:category/:id',
    login: '/login',
};

export default routes;
